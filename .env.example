# Only needed for deploying with the GenLayer CLI.
# The test suite needs none of this: pytest tests/ -q runs offline.
GENLAYER_NETWORK=studionet
# Deployment for this repository is done by hand through the Studio web
# interface. Never put a private key in a file.
