# gltest configuration
#
# Each contract is a single standalone GenVM file in contracts/.
# There is no build step: each is deployed exactly as written.
networks:
  default: studionet
  studionet: {}

paths:
  contracts: contracts
  artifacts: artifacts
